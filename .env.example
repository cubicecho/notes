# Server
PORT=4000
APP_URL=http://localhost:8081

# Client (Expo only exposes EXPO_PUBLIC_* vars to the browser bundle)
# Keep EXPO_PUBLIC_PORT in sync with PORT above.
EXPO_PUBLIC_PORT=4000

# Database (leave unset to use embedded PGLite)
# DATABASE_URL=postgres://user:pass@localhost:5432/notes
PGLITE_DATA_DIR=./pgdata

# Auth — placeholder user ID for development (no login flow in MVP)
DEMO_USER_ID=00000000-0000-0000-0000-000000000001
